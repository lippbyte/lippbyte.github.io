========================================================================
IMPLEMENTATION PLAN: PORTFOLIO 2.0 (MUHAMMAD KHALIFA)
Stack: HTML5, Tailwind CSS (via CDN/CLI), Vanilla JavaScript, FontAwesome
========================================================================

[PHASE 1: ENVIRONMENT & CONFIGURATION]
------------------------------------------------------------------------
1. Buat struktur folder proyek:
   ├── index.html
   ├── DESIGN.md
   ├── assets/
   │   ├── img/          <-- Simpan foto profil transparan di sini
   │   └── js/
   │       └── main.js   <-- Untuk logic popup & spotlight effect
   └── css/
       └── custom.css    <-- Untuk custom utility/animasi murni
2. Setup Tailwind CSS (Direkomendasikan via Tailwind CLI untuk produksi, 
   atau CDN script tag untuk prototyping cepat).
3. Inject Google Fonts: 'Space Grotesk' (Display/Heading) & 'Urbanist' (Body).

[PHASE 2: MARKUP & CONTENT INJECTION (SINGLE PAGE)]
------------------------------------------------------------------------
1. HEADER & HERO:
   - Buat navbar transparan (blur-md) yang sticky di atas.
   - Pasang foto profile tanpa border (wajib background transparan, format .png/.webp).
   - Inject copywriting Gen Z: Sat-set, percaya diri, straight to the point.
2. ABOUT ME:
   - Layout flex/grid mobile-first. Foto center di atas pada mobile, berdampingan pada desktop.
3. SKILLS & TOOLS:
   - Bagi kontainer jadi 2 section besar: Programming Tools & Workspace Tools.
   - Gunakan layout Grid Tailwind (`grid-cols-1 md:grid-cols-3`).
4. PROJECT SHOWCASE:
   - Render 3 kartu project utama.
   - Pasang mockup/screenshot project + tombol direct ke GitHub.
   - Tambahkan button "Lihat Semua Project" dengan identifier target untuk popup.
5. LEARNING JOURNEY:
   - Buat visualisasi timeline vertikal (mobile-friendly).
   - Tambahkan seksi khusus highlight/banner promosi lippbyte-blog.vercel.app.
6. CONTACT:
   - Gunakan form minimalis yang sudah kamu miliki dengan penyesuaian kelas Tailwind.

[PHASE 3: INTERACTIVITY & MICRO-INTERACTIONS (main.js)]
------------------------------------------------------------------------
1. IMPLEMENTASI POPUP MODAL (Lihat Semua Project):
   - Buat div overlay `#project-modal` dengan status default `hidden` dan `opacity-0`.
   - Buat event listener `onClick` pada button "Lihat Semua".
   - Saat diklik, hapus kelas `hidden` dan ganti jadi `flex`, lalu animasikan opacity ke `100`.
   - Sediakan tombol close (`X`) atau close on backdrop click.
2. EFREK SPOTLIGHT CARD (60fps.design style):
   - Gunakan event listener `mousemove` pada class `.spotlight-card`.
   - Hitung koordinat X dan Y kursor terhadap kartu.
   - Update CSS custom property `--mouse-x` dan `--mouse-y` secara dinamis untuk menggeser gradien glow di background card.

[PHASE 4: TESTING, OPTIMIZATION & DEPLOYMENT]
------------------------------------------------------------------------
1. Responsive Check: Pastikan tidak ada horizontal scrolling pada resolusi 320px - 425px (Mobile).
2. Compression: Kompres foto profil dan screenshot project agar berukuran < 150kb.
3. Deploy ke GitHub Pages (karena ini file statis index.html, proses deploy akan instan).